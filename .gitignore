# Python
__pycache__/
*.pyc
.venv/
*.egg-info/
dist/
build/

# Local work files (see CLAUDE.md conventions: never use /tmp)
tmp/

# Sparse git clones of the shuttle repos, rebuilt by `uv run tt-fetch`
cache/

# Generated SQLite database, rebuilt by `uv run tt-build-db`
*.db
*.db-journal

# Static site build, rebuilt by `uv run tt-build-site` (deployed by GitHub Actions)
site/
